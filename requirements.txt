# Core API framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Environment and configuration
python-dotenv>=1.0.0

# Image processing (compatible versions)
pillow>=10.0.0
# Note: OpenCV removed - not needed for core functionality
# opencv-python-headless can be installed separately if needed

# Utilities and data processing
numpy>=1.25.0
requests>=2.31.0
pandas>=2.1.0
jsonschema>=4.20.0

# UI framework
streamlit>=1.28.0

# AI integration
openai>=1.0.0
pydantic>=2.5.0

# File processing
openpyxl>=3.1.0
python-docx>=1.1.0

# System monitoring
psutil>=5.9.0

# Optional: OCR (install separately if needed)
# pytesseract>=0.3.10
